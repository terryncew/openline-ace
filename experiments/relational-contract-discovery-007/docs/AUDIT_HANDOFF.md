# RCDL-007 audit handoff

## Frozen verdict

`PRE_ADJUDICATION_CAUSAL_PARITY`

Claim effect: `UNIQUE_PRE_ADJUDICATION_UTILITY_NOT_FOUND`

## Audit targets

The audit should try to falsify five boundaries:

1. evaluation policies cannot access hidden fault sets or standing labels;
2. no `artifact_valid`, recovery horizon, relation name, or verdict-derived
   feature enters evaluation policy inputs;
3. the learned development table is action-complete over all ten opaque probes;
4. both policies use the same hard budget and minimax probe selector;
5. ledger and queue adapters preserve the causal success bit while varying
   nuisance surface state.

The canonical evaluation package contains 640 rows. Each policy has 320 rows,
320 correct decisions, and 544 counted probes. The evidence index binds every
frozen evidence artifact by SHA-256. Policy authority is `NONE`.

A successful audit may narrow or invalidate the result. It may not promote it
to live authority.
