from pathlib import Path
import argparse,json,os
ap=argparse.ArgumentParser(); ap.add_argument('--result',required=True); a=ap.parse_args(); r=json.loads(Path(a.result).read_text()); m=r['metrics']; i=r['integrity']; lines=['# FAR-005','',f"**Verdict:** `{r['verdict']}`",'',f"**Standing:** `{r['scientific_standing']}`",'',f"**Ordered unblocking coverage:** `{m['ordered_unblocking_coverage']:.3f}`",'',f"**Remote-regression rejection:** `{m['remote_regression_rejection_rate']:.3f}`",'',f"**Causal receipt binding:** `{m['causal_receipt_binding_rate']:.3f}`",'',f"**Recall coverage / precision:** `{m['recall_coverage']:.3f}` / `{m['recall_precision']:.3f}`",'',f"**Post-recall pipeline reliance blocked:** `{m['post_recall_reliance_block_rate']:.3f}`",'',f"**Authority escape admission:** `{m['authority_escape_admission_rate']:.3f}`",'',f"**Post-saturation rejection:** `{m['post_saturation_rejection_rate']:.3f}`",'',f"**Mean terminal genuine progress:** `{m['mean_terminal_genuine_progress']:+.3f}`",'',f"**Generator commit rate:** `{m['generator_commit_rate']:.3f}`",'',f"**Progress evaluator queried during search:** `{i['progress_evaluator_queries_during_search']}`",'',f"**HALT_SATURATED active:** `{i['halt_saturated_active']}`"]
Path(os.environ['GITHUB_STEP_SUMMARY']).write_text('\n'.join(lines)+'\n')
