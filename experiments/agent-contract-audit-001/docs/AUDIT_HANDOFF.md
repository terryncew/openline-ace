# A-001 Audit Handoff

## Frozen conformance targets

The checked mechanics must produce:

- `validated-artifact-binding` → `SUPPORTED`;
- `format-scratchpad-ritual` → `REJECTED_RITUAL`;
- `generic-context-disturbance` → `UNDECIDABLE_SHAM_EFFECT`;
- `wrapper-audit-marker-rule` → `REJECTED_RITUAL`.

## Attack priorities

An independent audit should attempt to:

1. inject forbidden adjudication fields through nested proposer output;
2. substitute wrapper-local status for original verifier success;
3. drop an arm from a pair;
4. change verifier identity within a pair;
5. tamper with frozen evidence after indexing;
6. reorder rows and mutate nuisance-only runner metadata;
7. force scientific promotion from the conformance fixture;
8. reduce the live minimum below 64 paired rollouts.

A successful attack narrows or invalidates the mechanism claim. It cannot be
converted into policy authority.
