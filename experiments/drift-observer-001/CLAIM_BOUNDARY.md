# Claim Boundary

Frozen positive claim:

The Drift Observer can compare a current state with an immutable verified baseline, preserve displacement as a per-dimension vector, detect expiration or revocation of the reference separately from metric drift, and produce dependency-scoped REOPEN / RETAIN / ACE_RECOMMENDED projections without granting execution authority.

Forbidden upgrades:

- Δhol predicts failure.
- Δhol is a universal coherence metric.
- A threshold crossing proves unsafe behavior.
- The display scalar is an authority signal.
- Receiver-selected domain dimensions are guaranteed complete.
- ACE must run after every drift event.
- Successful re-verification mutates or resurrects the old baseline.
