# Claim Boundary

Positive claim available if the held-out run passes:

> On the frozen two-repository replay corpus, dependency-aware selective reopening matched every warranted reopening and retention encoded by the independent held-out oracle, while global invalidation performed unnecessary reviews and flat latest-state missed descendant reopenings.

Forbidden upgrades: dependency discovery is solved; the graph is complete; two repositories estimate production-wide performance; state change implies factual error, danger, or lack of authorization; observer output grants runtime permission.

The next escalation is a larger automatically sampled external corpus with dependency declarations frozen before event selection.
