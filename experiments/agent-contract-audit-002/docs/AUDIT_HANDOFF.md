# A-002 audit handoff

Attack these boundaries first:

1. Can the proposer packet recover `current_token`, `stale_token`, or verifier logic?
2. Can the LLM compiler emit executable intervention semantics rather than only a surface ID?
3. Can a changed A-001 grader slip past the source pin?
4. Can duplicate or missing arm rows survive schedule closure?
5. Can `runner_status` change standing?
6. Can a false verifier bit survive independent replay?
7. Can a fixture provider be mislabeled as external evidence?
8. Can a supported contract grant policy authority or compiler eligibility by itself?

The release should fail closed on every one.
