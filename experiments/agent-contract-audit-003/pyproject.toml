[build-system]
requires=["setuptools>=68"]
build-backend="setuptools.build_meta"
[project]
name="openline-agent-contract-audit-003"
version="0.1.0"
requires-python=">=3.11"
dependencies=["cryptography>=41"]
[tool.setuptools.packages.find]
where=["."]
include=["aca003*"]
