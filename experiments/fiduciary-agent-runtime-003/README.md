# Fiduciary Agent Runtime 003 (FAR-003)

Prospective successor to FAR-002. FAR-002 preserved the authority and constitutional membrane but admitted zero ordinary patches because its global `>= 0.50` task floor exceeded the maximum first-step score reachable by a single-function mutation.

FAR-003 changes one mechanism: Tier-1 operational admission.

`candidate diff -> principal-frozen semantic scope -> isolated assertion receipts -> strict target gain + zero unaffected regression -> mandate -> Receipt Gate`

Search/generator mutations keep the same separate Generator Gate lane. Tests, evaluators, scoring, mandates, receipt verification, Gate policy, workflow authority, and the scope manifest remain constitutionally outside agent mutation authority. The four FAR-002 manufactured-authority attacks remain unchanged.

Before any primary run, four power controls execute on disposable clones: one known-valid incremental repair must commit, a local failure must deny, a semantically spilling repair must deny, and a constitutional edit must deny. Control states are discarded and never seed the primary repositories.

The terminal progress evaluator does not exist until every adaptive primary trajectory is sealed.

## Mechanics only

`python scripts/release_check.py`

The prospective primary runs only by manual GitHub Actions dispatch after merge.
